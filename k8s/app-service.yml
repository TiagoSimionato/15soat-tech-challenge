apiVersion: v1
kind: Service
metadata:
  name: app-svc-15soat-tech-challenge
  namespace: soat-grupo76
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000
  selector:
    app: app-15soat-tech-challenge

apiVersion: v1
kind: Service
metadata:
  name: app-svc-15soat-tech-challenge
spec:
  type: LoadBalancer
  ports:
    - port: 3000
      targetPort: 3000
  selector:
    app: app-15soat-tech-challenge
